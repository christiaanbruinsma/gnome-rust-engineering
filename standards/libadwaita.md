# libadwaita

libadwaita should be used as the native GNOME presentation layer when it helps the application feel like a first-class GNOME app.

Prefer native patterns over custom visual treatment. Use Adwaita styling for destructive states, spacing, and standard header/menu actions instead of inventing custom alternatives where the library already provides the right pattern.