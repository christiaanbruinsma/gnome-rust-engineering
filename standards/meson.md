# Meson

Meson is the GNOME project and installation layer for the suite.

Use it to configure application identities, generated desktop/AppStream files, installation layout, i18n integration, and the bridge to the Rust build.

Do not treat Cargo as a replacement for Meson in a GNOME desktop application.