# Rust

Rust is the default standalone application language for the suite when building native GNOME desktop tools.

Prefer explicit ownership, small borrow scopes, and clear module boundaries.

Rust code should be written so that GTK signal re-entry, background work, and file handling boundaries remain obvious and testable.